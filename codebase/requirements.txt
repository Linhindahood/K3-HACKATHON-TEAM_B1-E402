# Backend (FastAPI — lõi RAG)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# RAG
faiss-cpu>=1.8
numpy>=1.26

# LLM providers — API key thuần, không train model
openai>=1.40          # dùng chung cho OpenAI và OpenRouter (OpenRouter tương thích OpenAI API qua base_url riêng)
google-generativeai>=0.7   # Gemini

# Frontend debug UI
streamlit>=1.35
requests>=2.31

# Config & test
python-dotenv>=1.0
pytest>=8.0
