# Backend (FastAPI)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# RAG
bm25s==0.3.9
faiss-cpu>=1.8
numpy>=1.26
sentence-transformers[onnx]>=5.0,<6.0
Pillow>=12.0,<13.0
# GPU deployment: use sentence-transformers[onnx-gpu] instead of the ONNX CPU extra.

# LLM providers — API key thuần, không train model
openai>=1.66          # OpenAI Responses API + OpenRouter Chat Completions
google-genai>=1.0     # Gemini SDK hiện hành

# Discord bot
discord.py>=2.4
httpx>=0.27

# Frontend debug UI
streamlit>=1.35
requests>=2.31

# Config and tests
python-dotenv>=1.0
pytest>=8.0
